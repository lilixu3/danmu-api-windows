<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DanmuApi.App.ViewModels"
             xmlns:rt="using:DanmuApi.Runtime"
             xmlns:controls="using:DanmuApi.App.Controls"
             x:Class="DanmuApi.App.Views.DanmuDetailsView"
             x:DataType="vm:DanmuDetailsPageViewModel">
  <ScrollViewer VerticalScrollBarVisibility="Auto">
    <StackPanel Spacing="12">
      <Button Content="返回上一层" Classes="back-action" Command="{Binding BackCommand}" HorizontalAlignment="Left" />
      <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="16">
        <StackPanel Spacing="4">
          <TextBlock Text="弹幕详情" Classes="section-title" />
          <TextBlock Text="{Binding Title}" Classes="page-title" FontSize="22" TextWrapping="Wrap" />
          <TextBlock Text="{Binding SourceUrlText}" Classes="mono-text body-muted" TextWrapping="Wrap" />
          <StackPanel Orientation="Horizontal" Spacing="8">
            <Button x:Name="CopySourceButton" Content="复制来源地址" Classes="ghost-action" Command="{Binding CopySourceUrlCommand}" IsEnabled="{Binding HasSourceUrl}" />
            <Button x:Name="OpenSourceButton" Content="打开来源" Classes="ghost-action" Command="{Binding OpenSourceUrlCommand}" IsEnabled="{Binding HasSourceUrl}" />
            <ComboBox x:Name="ExportFormatComboBox"
                      Width="170"
                      ItemsSource="{Binding ExportFormatOptions}"
                      SelectedItem="{Binding SelectedExportFormatOption, Mode=TwoWay}"
                      IsEnabled="{Binding CanExport}" />
            <Button x:Name="ExportButton" Content="导出" Classes="secondary-action" Command="{Binding ExportCommand}" IsEnabled="{Binding CanExport}" />
          </StackPanel>
        </StackPanel>
        <StackPanel Grid.Column="1" Spacing="2">
          <TextBlock Text="{Binding CountText}" Classes="metric-value" />
          <TextBlock Text="当前筛选" Classes="metric-label" />
        </StackPanel>
        <StackPanel Grid.Column="2" Spacing="2">
          <TextBlock Text="{Binding DurationText}" Classes="metric-value" />
          <TextBlock Text="视频时长" Classes="metric-label" />
        </StackPanel>
      </Grid>

      <Border Classes="dashboard-panel" IsVisible="{Binding IsBusy}">
        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
          <ProgressBar IsIndeterminate="True" Width="180" Height="6" VerticalAlignment="Center" />
          <TextBlock Grid.Column="1" Text="{Binding LoadingText}" Classes="body-muted" VerticalAlignment="Center" TextWrapping="Wrap" />
        </Grid>
      </Border>

      <Border Classes="dashboard-panel" IsVisible="{Binding HasStatistics}">
        <StackPanel Spacing="8">
          <TextBlock Text="弹幕概览" Classes="section-title" />
          <Grid ColumnDefinitions="*,*,*,*" RowDefinitions="Auto,Auto" ColumnSpacing="12" RowSpacing="8">
            <TextBlock Text="{Binding Statistics.Count, StringFormat='弹幕：{0} 条'}" Classes="body-muted" />
            <TextBlock Grid.Column="1" Text="{Binding Statistics.DurationText, StringFormat='时长：{0}'}" Classes="body-muted" />
            <TextBlock Grid.Column="2" Text="{Binding Statistics.AverageText, StringFormat='平均：{0}'}" Classes="body-muted" />
            <TextBlock Grid.Column="3" Text="{Binding Statistics.RequestText, StringFormat='请求：{0}'}" Classes="body-muted" />
            <TextBlock Grid.Row="1" Text="{Binding Statistics.HotRangeText, StringFormat='最高密度：{0}'}" Classes="body-muted" />
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Statistics.HotCountText, StringFormat='区间数量：{0}'}" Classes="body-muted" />
            <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding Statistics.ColoredRatioText, StringFormat='彩色占比：{0}'}" Classes="body-muted" />
          </Grid>
        </StackPanel>
      </Border>

      <Border Classes="dashboard-panel" IsVisible="{Binding HasHighMoments}">
        <StackPanel Spacing="8">
          <TextBlock Text="高能时刻" Classes="section-title" />
          <ItemsControl ItemsSource="{Binding HighMoments}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate><WrapPanel Orientation="Horizontal" /></ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate x:DataType="rt:DanmuHeatmapBucket">
                <Button Classes="ghost-action"
                        Margin="0,0,8,6"
                        Command="{Binding $parent[ItemsControl].DataContext.SelectHeatmapBucketCommand}"
                        CommandParameter="{Binding}">
                  <StackPanel Spacing="2">
                    <TextBlock Text="{Binding RangeText}" Classes="mono-text" />
                    <TextBlock Text="{Binding CountText, StringFormat='{}{0}弹幕'}" Classes="body-muted" />
                  </StackPanel>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </StackPanel>
      </Border>

      <Border Classes="dashboard-panel">
        <StackPanel Spacing="8">
          <TextBlock Text="弹幕热力图" Classes="section-title" />
          <Grid IsVisible="{Binding HasHeatmap}" RowDefinitions="132,Auto,Auto" RowSpacing="8">
            <controls:DanmuHeatmapControl x:Name="HeatmapTimeline"
                                          Buckets="{Binding HeatmapBuckets}"
                                          SelectedIndex="{Binding SelectedHeatmapIndex, Mode=TwoWay}"
                                          HorizontalAlignment="Stretch" />
            <Grid Grid.Row="1" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
              <TextBlock Text="00:00" Classes="mono-text body-muted" />
              <TextBlock Grid.Column="1" Text="拖动时间轴查看区间" Classes="body-muted" HorizontalAlignment="Center" />
              <TextBlock Grid.Column="2" Text="{Binding Statistics.DurationText}" Classes="mono-text body-muted" />
            </Grid>
            <Grid Grid.Row="2" ColumnDefinitions="*,Auto" ColumnSpacing="12">
              <TextBlock Text="{Binding SelectedHeatmapRangeText}" Classes="mono-text" TextWrapping="Wrap" />
              <TextBlock Grid.Column="1" Text="{Binding SelectedHeatmapCountText}" FontWeight="SemiBold" />
            </Grid>
          </Grid>
          <TextBlock Text="暂无热力数据" Classes="body-muted" IsVisible="{Binding !HasHeatmap}" />
        </StackPanel>
      </Border>

      <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
        <TextBox Text="{Binding SearchQuery, Mode=TwoWay}" Watermark="搜索弹幕内容（支持空白分词）" />
        <ComboBox Grid.Column="1" Width="120" SelectedIndex="{Binding FilterIndex, Mode=TwoWay}">
          <ComboBoxItem Content="全部" />
          <ComboBoxItem Content="滚动" />
          <ComboBoxItem Content="顶部" />
          <ComboBoxItem Content="底部" />
        </ComboBox>
      </Grid>

      <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="8">
        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
          <TextBlock Text="{Binding Diagnostic}" Classes="body-muted" TextWrapping="Wrap" />
          <Button Grid.Column="1" Content="重新拉取" Classes="secondary-action" Command="{Binding LoadCommand}" IsVisible="{Binding CanRetry}" IsEnabled="{Binding !IsBusy}" />
        </Grid>
        <Border x:Name="EmptyFilterState" Grid.Row="1" Classes="muted-row" IsVisible="{Binding ShowEmptyFilterState}">
          <TextBlock Text="当前筛选下没有弹幕" Classes="body-muted" HorizontalAlignment="Center" />
        </Border>
        <ListBox Grid.Row="2" ItemsSource="{Binding DisplayedComments}" MaxHeight="520">
          <ListBox.ItemTemplate>
            <DataTemplate x:DataType="rt:DanmuComment">
              <Border Classes="muted-row" Margin="0,0,0,6" Padding="8">
                <Grid ColumnDefinitions="78,Auto,Auto,Auto,*" ColumnSpacing="8">
                  <TextBlock Text="{Binding TimeSeconds, StringFormat='{}{0:0.###} 秒'}" Classes="mono-text body-muted" />
                  <Ellipse Grid.Column="1" Width="12" Height="12" Fill="{Binding ColorHex}" VerticalAlignment="Center" />
                  <TextBlock Grid.Column="2" Text="{Binding ModeLabel}" Classes="body-muted" VerticalAlignment="Center" />
                  <TextBlock Grid.Column="3" Text="{Binding SourceLabel}" Classes="body-muted" VerticalAlignment="Center" />
                  <StackPanel Grid.Column="4" Spacing="2">
                    <TextBlock Text="{Binding Text}" TextWrapping="Wrap" />
                    <WrapPanel Orientation="Horizontal">
                      <TextBlock Text="{Binding ColorHex, StringFormat='颜色：{0}'}" Classes="body-muted" Margin="0,0,12,0" />
                      <TextBlock Text="{Binding CidText}" Classes="body-muted" Margin="0,0,12,0" />
                      <TextBlock Text="{Binding LikeText}" Classes="body-muted" Margin="0,0,12,0" />
                      <TextBlock Text="{Binding ColorV2Text}" Classes="body-muted" />
                    </WrapPanel>
                    <TextBlock Text="{Binding RawPositionText}" Classes="mono-text body-muted" TextWrapping="Wrap" />
                  </StackPanel>
                </Grid>
              </Border>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
      </Grid>
      <Button Content="加载更多" Classes="secondary-action" Command="{Binding LoadMoreCommand}" IsVisible="{Binding CanLoadMore}" HorizontalAlignment="Center" />
    </StackPanel>
  </ScrollViewer>
</UserControl>
