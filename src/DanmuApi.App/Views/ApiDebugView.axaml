<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DanmuApi.App.ViewModels"
             xmlns:controls="using:DanmuApi.App.Controls"
             x:Class="DanmuApi.App.Views.ApiDebugView"
             x:DataType="vm:ApiDebugPageViewModel">
  <Grid RowDefinitions="Auto,Auto,*" RowSpacing="12">
    <StackPanel Spacing="3">
      <TextBlock Text="接口调试" Classes="section" />
      <TextBlock Text="仅调用核心白名单接口；请求预览、CURL、界面响应与复制内容均为完整脱敏副本。" Classes="cap" TextWrapping="Wrap" />
    </StackPanel>
    <Grid Grid.Row="1" ColumnDefinitions="220,*,Auto,Auto" ColumnSpacing="10">
      <ComboBox ItemsSource="{Binding Definitions}" SelectedItem="{Binding SelectedApi, Mode=TwoWay}" />
      <TextBlock Grid.Column="1" Text="{Binding RequestSummary}" Classes="mono cap" VerticalAlignment="Center" />
      <Button x:Name="CancelRequestButton" Grid.Column="2" Content="取消" Classes="secondary" Command="{Binding CancelCommand}" IsVisible="{Binding CanCancel}" />
      <Button x:Name="ExecuteRequestButton" Grid.Column="3" Content="执行请求" Classes="primary" Command="{Binding ExecuteCommand}" IsEnabled="{Binding !IsBusy}" />
    </Grid>
    <Grid Grid.Row="2" ColumnDefinitions="360,*" ColumnSpacing="14">
      <Border Classes="card">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <StackPanel Spacing="10">
            <TextBlock Text="参数" Classes="section" />
            <ItemsControl x:Name="DynamicParameters" ItemsSource="{Binding ParameterValues}">
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:ApiParameterValueViewModel">
                  <StackPanel Spacing="4" Margin="0,0,0,6">
                    <TextBlock Text="{Binding Label}" FontWeight="SemiBold" />
                    <ComboBox ItemsSource="{Binding Options}" SelectedItem="{Binding Value, Mode=TwoWay}" IsVisible="{Binding IsSelect}" />
                    <TextBox Classes="field"
                             Text="{Binding Value, Mode=TwoWay}"
                             Watermark="{Binding Placeholder}"
                             IsVisible="{Binding !IsSelect}"
                             controls:EnterKeyCommand.Command="{Binding $parent[ItemsControl].((vm:ApiDebugPageViewModel)DataContext).ExecuteCommand}" />
                    <TextBlock Text="{Binding Placeholder}" Classes="cap" TextWrapping="Wrap" IsVisible="{Binding !IsSelect}" />
                  </StackPanel>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
            <StackPanel x:Name="RawBodyPanel" Spacing="4" IsVisible="{Binding HasRawBody}">
              <TextBlock Text="JSON 请求体" FontWeight="SemiBold" />
              <TextBox Classes="field mono" Text="{Binding JsonBody, Mode=TwoWay}" AcceptsReturn="True" MinHeight="180" TextWrapping="Wrap" />
              <TextBlock Text="分片接口要求直接填写 Segment 对象，不要包裹 segment 字段。" Classes="cap" TextWrapping="Wrap" />
            </StackPanel>
            <Border Classes="inset" IsVisible="{Binding HasRequestPreview}">
              <TextBlock Text="{Binding RequestPreview}" Classes="mono cap" TextWrapping="Wrap" />
            </Border>
            <Button x:Name="CopyCurlButton" Content="复制 CURL" Classes="ghost" Command="{Binding CopyCurlCommand}" IsEnabled="{Binding HasCurl}" HorizontalAlignment="Left" />
            <TextBox Classes="field mono" Text="{Binding CurlText}" IsReadOnly="True" TextWrapping="Wrap" MinHeight="60" />
          </StackPanel>
        </ScrollViewer>
      </Border>
      <Border Grid.Column="1" Classes="card">
        <Grid RowDefinitions="Auto,Auto,Auto,Auto,*" RowSpacing="8">
          <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="8">
            <TextBlock Text="安全响应副本" Classes="section" />
            <Button Grid.Column="1" Content="复制响应" Classes="ghost" Command="{Binding CopyResponseCommand}" IsEnabled="{Binding HasResponse}" />
            <Button Grid.Column="2" Content="清空" Classes="ghost" Command="{Binding ClearResponseCommand}" />
          </Grid>
          <Border Grid.Row="1" Classes="rule" />
          <TextBlock Grid.Row="2" Text="{Binding ResponseMeta}" Classes="cap" TextWrapping="Wrap" />
          <TextBlock Grid.Row="3" Text="{Binding Diagnostic}" Classes="cap" TextWrapping="Wrap" />
          <Grid Grid.Row="4" RowDefinitions="Auto,*" RowSpacing="6">
            <Button Content="{Binding ToggleResponseText}" Classes="ghost" HorizontalAlignment="Left" Command="{Binding ToggleResponseCommand}" IsVisible="{Binding HasResponse}" />
            <TextBox Grid.Row="1" Classes="field mono" Text="{Binding ResponseDisplay}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" />
          </Grid>
        </Grid>
      </Border>
    </Grid>
  </Grid>
</UserControl>
